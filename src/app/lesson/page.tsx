import { RoutePlaceholder } from "@/components/route-placeholder";

export default function LessonPage() {
  return (
    <RoutePlaceholder
      route="/lesson"
      description="The adaptive teaching surface. The orchestrator will drive every step here from validated teaching decisions. Not built yet."
    />
  );
}
