import { RoutePlaceholder } from "@/components/route-placeholder";

export default function SetupPage() {
  return (
    <RoutePlaceholder
      route="/setup"
      description="Where a learner will define their goal, bring source material, and take a placement check. Not built yet."
    />
  );
}
